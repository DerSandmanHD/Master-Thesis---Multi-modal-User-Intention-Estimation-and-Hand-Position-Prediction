# Archive

Dieser Ordner enthält historischen Projektcode, der nicht von der aktuellen
Pipeline importiert oder durch aktive Cluster-Jobs aufgerufen wird.

- `prototypes/data_collection/`: frühe Machbarkeits- und API-Experimente
- `legacy_flat_object_baseline/`: erste, inzwischen abgelöste Trainingsbaseline

Archivdateien sind nur zur Nachvollziehbarkeit vorhanden. Neue Entwicklung
findet ausschließlich unter `Code/`, `Training/` und `tests/` statt.
